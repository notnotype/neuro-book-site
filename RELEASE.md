# Release Notes

## 0.1.0 - 2026-07-31

NeuroBook 官方站私有内测版本，提供账号关联、创意工坊和加密云备份。现在可以使用管理员发放的注册码创建账号；GitHub 登录仍未开放。

### 新功能

- 使用有效注册码创建账号；注册码可以由管理员设置使用次数、过期时间和停用状态。
- 注册时分别设置账号名和显示名称：账号名用于登录和个人主页，显示名称可以使用中文并在之后修改。
- 在页面顶部切换简体中文或 English；选择会在当前浏览器保存，切换时不会离开正在浏览或编辑的页面。
- 关联 NeuroBook 后，可以从应用发布和管理 skill、workflow、profile，并在站点逐个浏览或下载包内文件。
- 上传客户端已加密的云备份，并按实例查看、下载或删除；站点无法读取备份内容。
- 查看服务是否可用，并在存储空间不足时继续登录和删除旧文件。
- 管理员可以批量创建注册码，选择不限次数或限制次数，并设置过期时间、备注和停用状态。
- 已登录用户可以创建自己的邀请码，并分享同时带注册码和邀请码的注册链接；邀请码只记录邀请来源，不能替代注册码。

### 改进

- 注册表单会把问题标在对应输入框，并在提交失败后把焦点移到第一个需要修改的位置；不再直接显示难懂的英文校验信息。
- 登录、账号设置、管理控制台、发布工作台和错误页面都支持中英文；日期、数字和文件大小会按当前语言显示。
- 限制上传大小、解压内容和请求频率，避免异常压缩包或并发上传耗尽服务器。
- 只从本机回环地址提供服务，由宿主 HTTPS 入口转发；容器使用非管理员账号和只读系统目录运行。

### 升级须知

- 首次安装必须生成并保存管理员密码；服务没有默认密码，也不会从环境变量读取管理员密码。
- 第一次云备份必须把恢复码保存在本实例之外。恢复码和本地数据同时丢失时，云端备份无法恢复。
- 升级时，旧的一次性邀请码会变成不限次数注册码，并保留已有注册归属。部署者仍需显式开启密码注册；GitHub 登录保持关闭。
